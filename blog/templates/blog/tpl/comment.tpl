<div class="comment">
    <div class="info"><span class="author">{{ comment.author }}</span> on <span class="date">{{ comment.created }}</span></div>
    <div class="text">{{ comment.text }}</div>
</div>
